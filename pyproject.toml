[project]
name = "kortex-mini-codex"
version = "0.1.0"
description = "Local mini-Codex TUI with human-in-the-loop execution"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
  "textual>=0.58.0,<1.0.0",
]

[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"
